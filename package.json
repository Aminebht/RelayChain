{
  "name": "relaychain",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:local:sync": "cross-env SYNC_FRONTEND_ADDRESSES=true hardhat run scripts/deploy.js --network localhost",
    "sync:frontend:localhost": "node scripts/sync-frontend-addresses.js localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "cross-env": "^7.0.3",
    "hardhat": "^2.22.10"
  }
}
